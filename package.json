{
  "name": "amkyaw-ai-worker",
  "version": "1.0.0",
  "description": "AI Worker gateway for Amkyaw bot",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
